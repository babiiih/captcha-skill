{
  "postCreateCommand": "cd /workspaces/captcha-skill && tar -xzf xai-register.tar.gz && pip install curl_cffi requests && python3 -c \"from curl_cffi import requests; print('ok')\" && echo 'READY' > /tmp/xai_ready.txt"
}
